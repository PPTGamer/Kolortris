/**
 *	Abstract class containing common constants. Used by {@link GameServer GameServer} and {@link GameClient GameClient}.
**/
abstract class NetworkingComponent{
	protected final int PORT_NUMBER = 1337;
}